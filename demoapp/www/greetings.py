# import frappe

# def get_context(context):
#     # Fetch the 'name' parameter from the URL
#     name = frappe.form_dict.get("name", "Guest")  # Default to 'Guest' if no name is provided

#     # Pass it to the context
#     context.name = name
